import React from 'react';
import st from './Settings.module.css';

const Settings = () => {
    return (
        <div className={st.settingsBlock}>
            Settings
        </div>
    );
}

export default Settings;