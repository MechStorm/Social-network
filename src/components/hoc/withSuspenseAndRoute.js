/*
import React from "react";

export const withSuspenseAndRoute = Component => {
    return (props) => {
        return <React.Suspense fallback={<div>Loading...</div>}>            optional HOC (may used in App.js inside Route - components)
                <Component {...props} />
            </React.Suspense>
    }
};*/